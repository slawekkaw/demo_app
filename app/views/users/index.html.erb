<h1>Listing users</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Lista', user %></td>
    <td><%= link_to 'Edycja', edit_user_path(user) %></td>
    <td><%= link_to 'Usuń', user, method: :delete, data: { confirm: 'Jesteś pewny?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
